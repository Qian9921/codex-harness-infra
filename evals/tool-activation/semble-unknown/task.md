Bounded-search this directory for an implementation that mixes a sliding
window of integers into one integer. If keyword search is insufficient and
Semble is configured, run one focused `semble search` on this directory and
inspect the top files. If Semble is missing or unhelpful, continue with
baseline reads. Do not treat query echo or readiness as the answer.
