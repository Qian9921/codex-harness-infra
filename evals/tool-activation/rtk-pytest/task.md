Run pytest on `test_sample.py` and summarize the outcome. If RTK is configured,
`rtk test` may compact the supported test output. Preserve the failure; do not
rewrite the command into an unrelated wrapper. If RTK is missing, run pytest
directly.
