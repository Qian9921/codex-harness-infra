[project]
name = "codex-harness-infra"
version = "23.1.1"
description = "A small, portable Codex delivery harness."
requires-python = ">=3.11"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
