# This is a local-only example. Copy it outside the repository and fill it with
# your machine's model IDs, GitHub identities, and optional tool paths.

[opening]
instruction = ""

[runtime]
# Optional but recommended: an absolute Python 3.11+ path used by the prompt hook.
python = ""

[models]
# Local environment data only. Recommended mapping: primary gpt-6-astra high,
# independent reviewer gpt-5.6-sol, quota-only native fallback Luna low.
# Shared templates remain model-agnostic placeholders.
primary = ""
primary_effort = "high"
executor = ""
# Native quota-exhaustion-only fallback; normal execution uses Grok Build low.
executor_effort = "low"
reviewer = ""

[github]
author_login = ""
author_config_dir = ""
reviewer_login = ""
reviewer_config_dir = ""

[tools]
# Optional paths used when a task needs them, and by explicit Doctor probes.
codegraph = ""
semble = ""
rtk = ""
